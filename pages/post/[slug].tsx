import React from 'react';
import {sanityClient, urlFor} from '../../sanity';

const Post = () => {
    return (
        <div>

        </div>
    );
};

export default Post;

export const getStaticPaths = async () => {

}
